<?php

use Fisharebest\Webtrees\I18N;

?>

<form method="post" action="<?= e(route('module', ['module' => '_jc-fancy-treeview_', 'action' => 'RemoveMenuItem', 'tree' => $tree->name(), 'xref' => $xref, 'type' => $type])) ?>" class="d-inline">
    <?= csrf_field() ?>
    <button type="submit" class="<?= $class ?>">
    <?= I18N::translate('Remove from menu') ?>
    </button>
</form>
